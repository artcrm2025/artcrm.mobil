import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent, Expo'nun kullanımı için gereken kök bileşenini kaydeder
// Bu, React Native'in AppRegistry.registerComponent() işlevine benzer
registerRootComponent(App);